#include<bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int q;cin >> q;
    while(q--){
        long double n;
        cin >> n;
        cout << fixed << setprecision(0) << pow(3,n) << "\n";
    }
    return 0;
}
